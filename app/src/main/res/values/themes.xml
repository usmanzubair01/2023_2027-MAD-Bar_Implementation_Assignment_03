<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme using Material 3 -->
    <style name="Base.Theme.AndroidAppBarImplementation" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorSecondary">@color/teal_200</item>
        <!-- Ensure status bar is handled by the app -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.AndroidAppBarImplementation" parent="Base.Theme.AndroidAppBarImplementation" />

    <!-- Theme with ActionBar for ActionBarActivity -->
    <style name="Theme.AndroidAppBarImplementation.WithActionBar" parent="Theme.Material3.DayNight">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="colorPrimary">@color/purple_500</item>
    </style>
</resources>